name: synthetic_api_cli
description: Declarative mock API server for frontend development
version: 1.0.1
repository: https://github.com/mubharaq/synthetic_api_cli.git

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.7.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  synthetic-api: synthetic_api
